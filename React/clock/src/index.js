import React from 'react';
import ReactDOM from 'react-dom';
import ClockApp from './clock/TheClockApp';


ReactDOM.render(
    <React.StrictMode>
        <ClockApp />
  </React.StrictMode>,
  document.getElementById('root')
);

